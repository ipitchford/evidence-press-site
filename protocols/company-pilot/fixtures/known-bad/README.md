# Known-bad controls

Each JSON file identifies one mutation of an otherwise valid object and the
specific failure the validator must detect. tools/pilot-tests.js applies every
mutation and fails if it is accepted. Controls cover governance, feasibility
stage, task-bank and allocation integrity, complete-roster missingness, quality
arithmetic and reliability sampling, resource measurement, and follow-up.
These are discrimination controls, not example pilot data.
