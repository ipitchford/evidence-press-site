{
  "name": "evidence-press-site",
  "private": true,
  "scripts": {
    "test:release-corpus": "playwright test --config playwright.release-corpus.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
