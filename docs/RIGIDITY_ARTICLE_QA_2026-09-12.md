# Popular rigidity article: editorial and media checks

Article: `when-polynomials-cannot-hide`, 12 September 2026.

This is a new communication object, not a new research release. It reuses the
completed internal source audit in `RIGIDITY_EXTERNAL_REVIEW_2026-09-12.md`.
No research archive, DOI, assurance rating or existing release is changed.
The institutional byline is Evidence Press; AI assistance is disclosed. The
external manuscript remains attributed to liqsweep and linked at its reviewed
commit. No third-party source is redistributed or relicensed.

## Editorial check

Applied the requested anti-slop and Williams–Bizup clarity skills, including
the natural-prose reference. The revision follows the concrete example before
introducing contact order, puts actions in verbs, removes generic significance
claims, and separates the contact bound from its imported geometric consequences.
The article states no industrial application, unrestricted factorial theorem,
arbitrary-length closure classification, priority determination or unaffiliated
verification. Existing proof and implication reviews were reused within their
recorded scope; no new scientific review round was represented as independent.

## Diagrams

`node tools/make-rigidity-article-art.js` reproduces the two original SVGs.
The banner samples exactly `z` and `z-2*z^3+z^4` on a real interval using a
shared coordinate map. It is explicitly not a complex-basin simulation.
The coefficient diagram records the exact vector `[1,0,-2,1]`. Images were
visually inspected, including a legible 390-pixel mobile diagram. The generated
article showed 16 KaTeX elements, no KaTeX errors, both images loaded and zero
document overflow at 1440 and 390 pixels. The complete article corpus browser
contract passed four desktop/mobile tests.

## Full narration

OpenAI `gpt-4o-mini-tts`, house voice `fable`, through the existing speech CLI;
three paragraph-bounded inputs, assembled with the repository article helper.
Duration: 506.28 seconds. MP3: 4,050,860 bytes.
SHA-256: `c3d43a44db69b87f392be844ef5ec32748b32c9e987a176b7271814d85f11b92`.
The committed provenance file binds the source, spoken transcript and audio.
The narration contains the main text, expanded notation, qualifications and
synthetic-voice disclosure. It omits link URLs and the generated source list.
Its spoken illustration caption is semantically equivalent to the concise
visual caption. After removing a duplicated visual caption, unchanged speech
segments were reused only after checking input and byte hashes.

Full-file round trips used `gpt-4o-mini-transcribe` and `gpt-4o-transcribe`.
Raw normalized token similarity was approximately 95% in both; this is not a
word-accuracy or assurance score. Differences include z/zed, spoken numbers,
formula serialization, British/American spelling and proper names, plus some
short phrase ambiguities. Both retain the main paragraphs and candidate-status
qualifications. The second route resolves several first-route anomalies.
These automated checks do not attest perfect pronunciation, especially of the
unverified pseudonym or Furter, or exact spoken-word fidelity. No human
listening or independent audio audit is claimed. Decoding and provenance
checks passed. No operating-system voice was used.
